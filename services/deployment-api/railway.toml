[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/deployment-api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "4002"
NODE_ENV = "production"


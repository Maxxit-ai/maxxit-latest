[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/signal-api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "4003"
NODE_ENV = "production"


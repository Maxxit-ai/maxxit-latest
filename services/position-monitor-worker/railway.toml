[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/position-monitor-worker && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "5002"
WORKER_INTERVAL = "60000"
NODE_ENV = "production"


[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/agent-api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "4001"
NODE_ENV = "production"


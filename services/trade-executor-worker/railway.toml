[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd services/trade-executor-worker && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "5001"
WORKER_INTERVAL = "30000"
NODE_ENV = "production"


# =============================================================================
# .env.proof-worker.example
#
# Copy this to .env.proof-worker and fill in the values before deploying.
# When using EigenCompute's ecloud CLI, these are set during deployment:
#   - Private vars: keep the name as-is (encrypted at rest inside the TEE)
#   - Public vars:  append _PUBLIC to the key name
#
# Usage with ecloud:
#   ecloud compute app deploy
#   → Select "Build and deploy from Dockerfile"
#   → Point to Dockerfile.proof-worker
#   → Provide env vars when prompted (or use a .env file)
# =============================================================================

# ---- Required ----

# PostgreSQL connection string (must be externally reachable, e.g. Neon / Supabase)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# SP1 prover mode: "prove" for real ZK proofs, "execute" for dry-run/test
SP1_PROVER_MODE=prove

# Private key of the wallet that submits proofs on-chain
SP1_PRIVATE_KEY=0x...

# Address of the deployed TraderRegistry contract
POSITION_REGISTRY_ADDRESS=0x...

# Arbitrum Sepolia RPC endpoint
ARBITRUM_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc

# ---- Auto-set in Docker (no need to change) ----

# Path to the SP1 host binary inside the container
# SP1_HOST_BINARY=/app/sp1/target/release/ostium-trader-host
